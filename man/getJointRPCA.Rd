% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AllGenerics.R and R/getJointRPCA.R
\name{getJointRPCA}
\alias{getJointRPCA}
\alias{getJointRPCA,MultiAssayExperiment-method}
\alias{getJointRPCA,SingleCellExperiment-method}
\title{Joint robust principal component analysis}
\usage{
getJointRPCA(x, ...)

\S4method{getJointRPCA}{MultiAssayExperiment}(x, experiments, assay.types, ...)

\S4method{getJointRPCA}{SingleCellExperiment}(x, experiments, assay.types, ...)
}
\arguments{
\item{x}{\code{MultiAssayExperiment} or \code{SingleCellExperiment}.}

\item{experiments}{\code{Character vector} or \code{integer vector}. Names
or indices of more than one experiment selected from \code{x}.}

\item{assay.types}{\code{Character vector}. Names of the assays selected
from the experiments.}

\item{...}{Additional arguments passed to the RPCA calculation. Supported
arguments include \code{test.set}, \code{ncomponents},
\code{max.iterations}, \code{n.test.samples}, and \code{test.ratio}.}
}
\value{
A numeric matrix of sample scores with class \code{JointRPCA}. Attributes
contain the rotation, explained variance, lower-rank representation,
distances, reconstruction error, and per-table feature counts.
}
\description{
Runs joint robust principal component analysis on multiple selected
experiments or alternative experiments.
}
\details{
Joint RPCA estimates a shared low-rank representation across tables that
contain the same samples but may contain different features. Missing values
are handled using matrix completion based on the joint OptSpace algorithm.
The returned sample scores include model information in their attributes.

If \code{test.set} is supplied, the model is fitted on the remaining samples
and the requested samples are projected into the resulting space. If it is not
supplied, representative test samples are selected from the first table.
}
\examples{
samples <- paste0("sample", seq_len(10L))
first <- matrix(
    rnorm(40L), nrow = 4L,
    dimnames = list(paste0("feature", seq_len(4L)), samples)
)
second <- matrix(
    rnorm(30L), nrow = 3L,
    dimnames = list(paste0("feature", seq_len(3L)), samples)
)
mae <- MultiAssayExperiment::MultiAssayExperiment(
    experiments = MultiAssayExperiment::ExperimentList(
        first = SingleCellExperiment::SingleCellExperiment(
            assays = list(counts = first)
        ),
        second = SingleCellExperiment::SingleCellExperiment(
            assays = list(counts = second)
        )
    )
)
getJointRPCA(
    mae,
    experiments = c("first", "second"),
    assay.types = c("counts", "counts"),
    test.set = samples[1:2],
    ncomponents = 2L,
    max.iterations = 5L
)
}
\seealso{
\code{\link[vegan:optspace]{vegan::optspace}}
}
\author{
The Joint-RPCA method was adapted from the original Python-based
implementation in biocore/Gemelli by Bianca Cordazzo Vargas, Liat Shenhav,
and Cameron Martino. The R/Bioconductor implementation was subsequently
prepared by Aituar Bektanov, Sabuj Bhowmick, Tuomas Borman, and Leo Lahti.
}
\references{
Martino, C. and Shenhav, L. et al. (2020) Context-aware dimensionality
reduction deconvolutes gut microbial community dynamics. \emph{Nature
Biotechnology}. doi:10.1038/s41587-020-0660-7
}
